services:
  minutes:
    build: .
    restart: unless-stopped
    ports:
      - "8080:8080"
    env_file:
      - .env
    environment:
      DATA_DIR: /data
      PORT: "8080"
      NODE_ENV: production
    volumes:
      - minutes-data:/data

volumes:
  minutes-data:
